import 'package:flutter/material.dart';

class MainScreenPlaceholderPage extends StatelessWidget {
  const MainScreenPlaceholderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Content placeholder',
      ),
    );
  }
}
